fastapi==0.104.1
uvicorn[standard]==0.24.0
psutil==5.9.6
# Blockchain registry integrations are optional. Install web3 via
# `pip install -r node/requirements-web3.txt` when you need on-chain features.

